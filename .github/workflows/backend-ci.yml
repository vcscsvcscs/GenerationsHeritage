name: Backend Continuous Integration
on:
  push:
    paths:
      - "backend/**"

jobs:
  lint:
    uses: ./CI/go_lint.yml
    with:
      working-directory: 'backend'
  build:
    needs: lint
    uses: ./CI/go_build.yml
    with:
      working-directory: 'backend'
  test:
    needs: build
    uses: ./CI/go_test.yml
    with:
      working-directory: 'backend'